import { combineReducers } from "redux";
import Employee_Reducer from "./components/reducer/employeeReducer";

const rootReducer = combineReducers({
  Employee_Reducer,
});

export default rootReducer;
